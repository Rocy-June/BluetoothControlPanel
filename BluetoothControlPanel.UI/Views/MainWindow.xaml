<Window x:Class="BluetoothControlPanel.UI.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:BluetoothControlPanel.UI.ViewModels"
        mc:Ignorable="d"
        Title="{Binding ApplicationTitle}"
        Height="450"
        Width="800"
        MinHeight="400"
        MinWidth="600"
        d:DataContext="{d:DesignInstance vm:MainViewModel, IsDesignTimeCreatable=True}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
            <TextBlock Text="{Binding ApplicationTitle}"
                       FontSize="20"
                       FontWeight="Bold" />
            <TextBlock Text="{Binding StatusMessage}"
                       Margin="12,0,0,0"
                       VerticalAlignment="Center"
                       Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}" />
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,12,0,12">
            <Button Content="Refresh status"
                    Command="{Binding RefreshStatusCommand}"
                    Padding="12,6" />
        </StackPanel>

        <GroupBox Grid.Row="2" Header="Activity" Margin="0,8,0,0">
            <ListBox ItemsSource="{Binding LogEntries}" />
        </GroupBox>
    </Grid>
</Window>
